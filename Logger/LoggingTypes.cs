using System;

/**
 * This enum contains logging level constraints.
 * 
 * @author nikoloz-code23
 * 
 */

namespace ConsoleLogProject
{
    public enum LogType
    {
        TRACE,
        DEBUG,
        INFO,
        WARN,
        ERROR,
        FATAL,
    }
}
