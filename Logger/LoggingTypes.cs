using System;

/**
 * This enum contains logging level constraints.
 * 
 * @author ltrlynick
 * 
 */

namespace ConsoleLogProject
{
    public enum LogType
    {
        DEBUG,
        INFO,
        WARN,
        ERROR,
        FATAL,
    }
}