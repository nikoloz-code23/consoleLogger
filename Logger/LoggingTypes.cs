using System;

/**
 * This enum contains logging level constraints.
 * 
 * @author ltrlynick
 * 
 */

namespace ConsoleLogProject
{
    public enum LogType
    {
        TRACE,
        DEBUG,
        INFO,
        WARN,
        ERROR,
        FATAL,
    }
}